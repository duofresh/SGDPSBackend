version: '3.8'

services:
  sgdp-backend:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - JWT_SECRET=your_jwt_secret_here
    volumes:
      - ./data:/app/data
    restart: unless-stopped 